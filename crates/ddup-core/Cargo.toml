[package]
name = "ddup-core"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "1.0.69"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.145"

[lints]
workspace = true
